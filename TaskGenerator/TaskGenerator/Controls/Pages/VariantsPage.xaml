<UserControl x:Class="TaskGenerator.Controls.Pages.VariantsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskGenerator.Controls.Pages"
             xmlns:views="clr-namespace:TaskGenerator.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="540" d:DesignWidth="330.828">
    <Grid
    Background="{StaticResource BackgroundBrush}">

    <Grid.RowDefinitions>
			<RowDefinition Height="{Binding ActualHeight, ElementName=label, Mode=OneWay}" />
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Label x:Name="label" 
		  Grid.Row="0"
      Content="Варианты"
      FontFamily="{StaticResource Inter}"
      FontSize="48"
      VerticalAlignment="Top"
      FontWeight="Bold"
      Padding="16,16,16,16"
      />

		<ScrollViewer Grid.Row="1">
			<StackPanel>
			</StackPanel>
		</ScrollViewer>
	</Grid>
</UserControl>
